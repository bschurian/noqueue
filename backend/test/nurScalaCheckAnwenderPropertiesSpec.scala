import org.scalacheck.Properties
import org.scalacheck.Prop._

/**
 * Created by anwender on 21.08.2017.
 */
class nurScalaCheckAnwenderPropertiesSpec extends Properties("ListSpec") {
  /*property("append") =
    forAll { (xs: List[Int], n: Int) =>
      collect(n)(
        (xs :+ n).last == n
      )
    }

  property("insert") =
    forAll { (xs: List[Int], n: Int) =>
      (n +: xs).head == n
    }*/
}
